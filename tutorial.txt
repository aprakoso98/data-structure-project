install Git, Tortoise Git, nodejs -> all Exe / msi

open cmd -> run this
npm install -g grunt
npm install -g grunt-cli
npm install -g bower
npm install -g sass
npm install -g yo
npm install -g generator-karma
npm install -g generator-angular

clone repository from this site https://github.com/aprakoso98/data-structure-project
open folder in cmd
run this -> npm install